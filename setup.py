from setuptools import setup, find_packages
setup(name="core", version="0.0", package_dir={"core": "."})
# setup(name="core", version="0.0", package_dir={"core": "src/core", "core.opt":"src/", "core.registry":"src/"})
